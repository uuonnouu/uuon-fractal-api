# uuon-fractal-api environment variables
# Rename this file to .env before running locally

# Redis URL for production rate limiting
# Leave blank on Replit — falls back to in-memory automatically
REDIS_URL=redis://localhost:6379

# API version label (informational only)
UUON_API_VERSION=1.0.0
